# Copy to .env and fill in real values before deploying anywhere but localhost.

JWT_SECRET=7bf1c15b3b23dd89f8fa641a3ba53f12626d50289382745f7d85b61badc59cc1
ENV=development

# Comma-separated origins allowed to call the API from a browser.
CORS_ORIGINS=["http://localhost:5173"]

# Only needed if USE_REDIS=true (multi-instance websocket fan-out).
USE_REDIS=false
